# Python (ملفات خاصة بلغة بايثون)
*.pyc             # ملفات البايتكود المترجمة
__pycache__/      # مجلدات ذاكرة التخزين المؤقت للبايثون
.Python/          # مجلدات قد تنتجها بعض بيئات البايثون

# Virtual Environment (البيئة الافتراضية - هذا أهم قسم)
env/
venv/             # هذا سيمنع مجلد البيئة الافتراضية "venv" من الرفع
.venv/            # بعض الأدوات قد تسمي البيئة الافتراضية هكذا
pip-wheel-metadata/

# Django (ملفات خاصة بإطار عمل Django)
*.log             # ملفات السجل (Logs)
*.pot             # ملفات الترجمة (عادةً لا ترفع، يتم إنشاؤها)
*.mo              # ملفات الترجمة المترجمة (عادةً لا ترفع)
*.xml
db.sqlite3        # ملف قاعدة بيانات SQLite المحلية - لا يجب رفعه أبدًا!
media/            # مجلد ملفات المستخدمين المرفوعة (صور، فيديوهات، إلخ) - لا يجب رفعه
static/           # إذا كنت تستخدم collectstatic لجمع الملفات هنا، فلا ترفع هذا المجلد

# IDEs (بيئات التطوير المتكاملة - إذا كنت تستخدمها)
.vscode/          # إعدادات VS Code الخاصة بك (اختياري: إذا كنت لا تريد مشاركتها)
.idea/            # إعدادات PyCharm أو IntelliJ IDEA
*.sublime-project
*.sublime-workspace

# Operating System Files (ملفات أنظمة التشغيل)
.DS_Store         # ملفات macOS
Thumbs.db         # ملفات Windows
ehthumbs.db
.directory

# Environment Variables (متغيرات البيئة - مهم جداً للبيانات الحساسة)
.env              # إذا كنت تستخدم ملف .env لتخزين مفاتيح API أو كلمات مرور